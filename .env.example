# ============================================================
# Grep vs RAG 实验配置文件
#   cp .env.example .env  然后填入你的配置
# ============================================================

# ── CLI Agent ──────────────────────────────────────────────
# 实验后端: claude,codex（逗号分隔，可选一个或多个）
BACKENDS=claude,codex

# CLI 使用的模型（留空=用 CLI 默认配置）
CLAUDE_CLI_MODEL=
CODEX_CLI_MODEL=

# CLI 超时（秒）
CLI_AGENT_TIMEOUT=300

# ── Embedding 模型（vector 检索使用）───────────────────────
# 本地模型，免费，首次运行自动下载 ~100MB
EMBEDDING_MODEL=BAAI/bge-small-zh
# 运行设备: cpu 或 cuda
EMBEDDING_DEVICE=cpu

# ── 检索参数 ───────────────────────────────────────────────
VECTOR_TOP_K=5
CHUNK_SIZE=300
CHUNK_OVERLAP=50

# ── 实验矩阵 ───────────────────────────────────────────────
DATASET_SIZES=small
TOOL_MODES=grep,vector

# ── 运行控制 ───────────────────────────────────────────────
LIMIT_QUESTIONS=          # 留空=全部, 填数字=只跑前N题
MAX_WORKERS=1             # 1=顺序, 4=4并发
RETRY_COUNT=3
